@keyframes open-animation
  from
    transform scale(0)
    opacity 0

  to
    transform scale(1)
    opacity 1

@keyframes rotate360
  0%
    transform rotate(0deg)

  100%
    transform rotate(360deg)
